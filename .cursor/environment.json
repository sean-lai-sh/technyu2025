{
  "name": "technyu2025",
  "user": "ubuntu",
  "install": "if ! command -v bun >/dev/null 2>&1 && [ ! -x \"$HOME/.bun/bin/bun\" ]; then curl -fsSL https://bun.sh/install | bash; fi; export PATH=\"$HOME/.bun/bin:$PATH\"; bun install --frozen-lockfile",
  "terminals": [
    {
      "name": "dev",
      "command": "export PATH=\"$HOME/.bun/bin:$PATH\"; NEXT_PUBLIC_SANITY_PROJECT_ID=\"${NEXT_PUBLIC_SANITY_PROJECT_ID:-placeholder}\" NEXT_PUBLIC_SANITY_DATASET=\"${NEXT_PUBLIC_SANITY_DATASET:-production}\" npm run dev"
    }
  ],
  "ports": [3000]
}
